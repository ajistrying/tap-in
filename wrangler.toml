name = "public-pkm-interface"
compatibility_date = "2025-01-01"
compatibility_flags = ["nodejs_compat"]

pages_build_output_dir = ".svelte-kit/cloudflare"

# KV namespace for rate limiting (optional - can use DB instead)
# [[kv_namespaces]]
# binding = "RATE_LIMIT_KV"
# id = "" # Fill in after creating KV namespace

[vars]
# Add non-secret environment variables here

# Secrets (set via wrangler secret put):
# - DATABASE_URL
# - DEEPSEEK_API_KEY
# - OPENROUTER_API_KEY
# - OPENROUTER_EMBEDDING_MODEL (optional)
# - OPENROUTER_CHAT_MODEL (optional)
